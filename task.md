In this project, we create a version of the game chess with only kings and pawns
Python and Flask to provide RESTful APIs, with Python’s built-in database (SQLite) used for data persistence.
The system supports the following functionalities:
Username creation
Bot difficulty
Changing from light or dark mode
Enable/disable timer
Finally, we will deploy the game through cloudfare, then people can access it publicly.